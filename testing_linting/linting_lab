"""Module to count the number of items in a sequence"""
def count(sequence, item):
    """Function that takes the parameters of sequence, item"""
    y = 0
    for n in sequence:
        if n == item:
            y += 1
    return y
